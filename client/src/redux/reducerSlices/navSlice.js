import { createSlice } from "@reduxjs/toolkit"

const initialState = {

}

const NavSliceReducer = createSlice{
    {
        name: 'nav'
        initialState: initialState
        reducers:{
            
        }
    }
}
